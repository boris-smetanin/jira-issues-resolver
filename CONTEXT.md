# Jira Issues Resolver

A locally-installed web app that drains a configured set of Jira issues by orchestrating an AI coding agent (via the Sandcastle library) to produce a pull request per issue. Distributed across team members' laptops; each install runs its own Spaces. The agent runs on the host by default; container isolation is opt-in per Space.

## Language

### Core domain

**Space**:
A real-world service with one GitHub repository and one Jira filter; the unit of configuration that ties "which Jira issues" to "which repo" for the agent.
_Avoid_: project, team, integration, workspace

**Jira Filter**:
The set of fields that select which Jira issues belong to a Space: project key, **filter field** (one of `component` / `labels` / `fixVersion` — built-in allowlist only), filter value, allowed statuses, and agent labels.
_Avoid_: query, JQL string

**Base Branch**:
The Space's GitHub branch that new issue branches are created off (defaults to `main`). Per-Space setting.
_Avoid_: master, default branch, trunk

**Target Status**:
The Jira status name an issue is transitioned into after a Resolve Attempt successfully opens its PR (e.g. "Code Review"). Per-Space setting. If no valid Jira transition exists from the issue's current status, the attempt is still `FINISHED` (soft-fail) — the PR is the valuable output.
_Avoid_: review status, done status

**Agent Label**:
A Jira label that gates whether the agent picks up an issue. An issue must carry at least one of the Space's agent labels to be eligible, even if every other filter matches. Defaults: `reopen`, `queued`.
_Avoid_: trigger label, tag

**Resolve Attempt**:
A single agent run that takes one Jira issue and tries to produce a PR. Immutable; reopens or retries create a new Resolve Attempt with `prior_attempt_id` pointing back.
_Avoid_: fix attempt, run, job, task

**Reopen**:
The situation where a new Resolve Attempt is created for a Jira issue that already has prior Resolve Attempts in terminal states. Not a flag; inferred from `prior_attempt_id != null`.
_Avoid_: retry, re-run

**Agent Runtime**:
Where the agent process executes for a Space. Per-Space `agent_runtime_mode` decides:
- **host** (default): Sandcastle's no-sandbox provider — agent runs as a subprocess of the server on the host, with host tooling and host credentials in scope. Fast setup, no isolation.
- **container**: Sandcastle's bind-mount provider — agent runs in a Docker container built from a per-Space Dockerfile stored in DB (`dockerfile_content`, generated by merging the repo's own Dockerfile with the Sandcastle agent layer; never committed to the repo). Slower first-build per Space, real isolation.
_Avoid_: sandbox, agent backend

**Worktree**:
The per-Resolve-Attempt git working directory created off the Space's persistent clone (`data/repos/<spaceId>/wt/<issueKey>/`). Lets multiple attempts coexist on disk without `checkout` thrash. Managed explicitly by the orchestrator; Sandcastle is pointed at it via `cwd` with `branchStrategy: 'head'`.
_Avoid_: checkout, working copy

**Agent Provider**:
The Sandcastle factory that builds the LLM-side agent: `claudeCode(model)` or `codex(model)`. Configured per Space. Default: `claudeCode('claude-opus-4-7')`.
_Avoid_: agent type, agent backend

**Committer Identity**:
Per-Space `name` + `email` stamped on commits via `git config user.name/user.email` inside the worktree before the agent runs. The email must be a verified address on the GitHub account owning the Space's PAT, otherwise commits appear un-linked on PRs.
_Avoid_: author, bot identity

**Resolve Loop**:
The per-Space autonomous activity engine that polls Jira on an interval and creates Resolve Attempts. Off by default at Space-create; started/stopped via UI. Persisted state (`loop_running` on the Space row) so it can auto-resume across server restarts.
_Avoid_: scheduler, poller, worker

**Tick**:
A single iteration of a **Resolve Loop**: one JQL search, one decision per matching issue (create new attempt or skip). A tick creates 0..N Resolve Attempts; it does not produce commits directly. Default interval: 300 seconds per Space.
_Avoid_: cycle, iteration, poll

### Resolve Attempt status

The state a Resolve Attempt is in. Terminal states are marked `■`.

- `QUEUED` — created but not started
- `PREPARING_REPO` — worktree being set up
- `AGENT_RUNNING` — the **Agent Provider** is executing inside the **Agent Runtime**
- `CHECKING_COMMITS` — agent returned; orchestrator inspecting the worktree
- `PUSHING` — pushing the new branch to GitHub
- `OPENING_PR` — creating the GitHub PR
- `TRANSITIONING_JIRA` — moving the Jira issue to code-review
- `FINISHED` ■ — PR open, Jira transitioned
- `FINISHED_NO_CHANGES` ■ — agent ran cleanly but produced no commits
- `FAILED` ■ — error at any step; `error_reason` and `stuck_at_status` captured

## Relationships

- A **Space** has one GitHub repo, one **Jira Filter**, and 0..N **Resolve Attempts**.
- A **Resolve Attempt** belongs to exactly one **Space** and references one Jira issue by key.
- A **Resolve Attempt** may link to a prior **Resolve Attempt** for the same `(space, issue)`; the existence of that link is what makes the new attempt a **Reopen**.
- A **Resolve Attempt** runs in one **Agent Runtime** on one **Worktree** off the Space's persistent clone.
- A **Space** has one **Base Branch** (default `main`), one **Target Status** (e.g. "Code Review"), one **Agent Provider**, one **Committer Identity**, and one **Resolve Loop**.
- A **Resolve Loop** produces **Ticks**; each Tick produces 0..N Resolve Attempts.
- A successful Resolve Attempt produces exactly one commit (squashed from whatever the agent did) on the issue's branch.
- The agent can read code and commit; it cannot push, open PRs, or talk to Jira — the server does those, outside the **Agent Runtime**.

## Example dialogue

> **Dev:** "If a **Resolve Attempt** finishes and someone transitions the Jira issue back to queued, do we mutate the row or make a new one?"
> **You:** "New **Resolve Attempt**. The prior one stays in `FINISHED`. The new one's `prior_attempt_id` points at it — that's what makes it a **Reopen**, and that's the signal to pull in the prior PR's review comments and the Jira comments added after the prior attempt ended."

> **Dev:** "What if the JQL matches an issue and there's already an attempt in `AGENT_RUNNING`?"
> **You:** "Skip. The 'no in-flight attempt' rule says: only create a new **Resolve Attempt** if every prior one is terminal."

> **Dev:** "Can the agent push the branch itself?"
> **You:** "No. The agent's surface is prompt context + the **Worktree**. Push, PR open, and Jira transition all happen outside the **Agent Runtime**, by the server."

## Flagged ambiguities

- "issue" vs. "ticket" — use **Jira issue** (or just *issue* when context is clear). Avoid *ticket*.
- "branch name" — by convention, the branch name equals the Jira issue key (e.g. `RND-1`). It is not a separately-configurable name.
- "reopen" the label vs. **Reopen** the situation — the label is one of the configured **Agent Labels**; the situation is the data condition `prior_attempt_id != null`. Both exist; don't conflate them in code or copy.
